import {notFound} from "next/navigation"; import Link from "next/link"; import ReactMarkdown from "react-markdown"; import {supabaseServer} from "@/lib/supabase-server";
export default async function Post({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const sb=await supabaseServer();const {data:p}=await sb.from("posts").select("*").eq("slug",slug).eq("published",true).single();if(!p)notFound();return <><header className="nav"><Link className="brand" href="/"><i className="brand-mark"/>The Learning Ledger</Link><Link className="back" href="/">← All entries</Link></header><main className="post-page"><div className="eyebrow">{p.category||"NOTE"} · {new Date(p.published_at||p.created_at).toLocaleDateString("en-GB")}</div><h1>{p.title}</h1><p className="lead">{p.excerpt}</p><hr style={{borderColor:"#2c2a27",margin:"35px 0"}}/><article className="post-content"><ReactMarkdown>{p.content}</ReactMarkdown></article></main></> }